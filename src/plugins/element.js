// 导入组件
import Vue from 'vue'
import { Button } from 'element-ui'
// 按需导入
import { Form, FormItem } from 'element-ui'
import { Input } from 'element-ui'

// 注册为全局可用组件
Vue.use(Button)
Vue.use(Form)
Vue.use(FormItem)
Vue.use(Input)